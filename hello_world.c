#include <stdio.h> // header - imports standard lib which has the `puts` function

int main(int argc, char** argv) { // output an int, takes as input an int and a char**
  puts("Hello, world111!!!"); // `puts` = put string. Outputs a string.
  return 0; // when a C program returns `0` this indicates that there have been no errors running the program
  }
